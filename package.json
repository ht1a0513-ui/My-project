{
  "name": "event-registration-system-root",
  "private": true,
  "scripts": {
    "dev": "npm --prefix \"event registration system\" run dev -- --host 0.0.0.0",
    "build": "npm --prefix \"event registration system\" run build",
    "start": "npm --prefix \"event registration system\" run dev -- --host 0.0.0.0",
    "backend": "npm --prefix \"backend\" run dev"
  }
}
